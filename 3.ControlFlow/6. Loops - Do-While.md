# loops - Do-While

The `do-while`tests the termination condition at the bottom, after making each pass through the loop body; the body is always executed at least once.

The default sytax of the `do` is:

```c
do
    statement
while (expression);
```
The statement is executed, then the expression is evaluated. 
