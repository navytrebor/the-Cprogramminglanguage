## Static Variables

The variables `sp`in `stack.c`, and `buf`and `bufp`in `getch.c`are for the private use of the functions in their respective source files, and are not meant to be accessed by anything else.
The **`static`** declaration, applied to external variable or function limits the scope of that object to the rest of the source file being compiled. 
